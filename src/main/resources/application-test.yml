spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true 시스템아웃을 통해 찍음
        format_sql: true
  datasource:
    url: jdbc:h2:tcp://localhost/~/hisquare
    username: sa
    password: 1234
    driver-class-name: org.h2.Driver